# Copy to .env and fill values. .env is gitignored.
EXPO_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Optional — paste Sentry project DSN to also send crashes to sentry.io
# Local crash vault works even when this is empty.
EXPO_PUBLIC_SENTRY_DSN=
